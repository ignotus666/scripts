#!/bin/sh


if [ -d ~/ardour ]
then
    rm -rf ardour
fi

cd ~

git clone git://github.com/Ardour/ardour

#sudo apt-get build-dep ardour
# except debian does not [yet] list libreadline as build-dependency
#sudo apt-get install libreadline-dev

cd ardour

./waf configure --optimize

./waf

#sudo ./waf install
#./gtk2_ardour/ardev
